# Local-only Neon stand-in (never commit real values).
# Pooled URL is used by the Worker via Hyperdrive in production.
DATABASE_URL=postgres://cyvra:cyvra@127.0.0.1:5432/cyvra_mobile
# Direct URL is used for Drizzle migrations only.
DATABASE_URL_DIRECT=postgres://cyvra:cyvra@127.0.0.1:5432/cyvra_mobile
