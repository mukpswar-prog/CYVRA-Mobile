# G0–G3 execution notes (this slice)

Governing law: [GUIDELINE.md](./GUIDELINE.md) (also at repo root). This file records
what G0–G3 require versus what is live.

## Gates vs this branch

| Gate | Guideline deliverable | Status |
|---|---|---|
| **G0** | Guideline in `docs/`. Repo private. GitHub description fixed. | Guideline is in `docs/GUIDELINE.md`. Repo is **private**. Description is still `Mobile hardware scanner & reporting` — `gh` is read-only here, so it was not changed. |
| **G1** | Pages `cyvra-mobile`, Worker `cyvra-mobile-api`, Hyperdrive `cyvra-mobile-neon` → Neon `floral-art-02749206`, Resend domain verified | Pages, Worker, and Hyperdrive exist on account `5a3eeb2b3d42726a8ba08732464a0eda`. Hyperdrive id is in `services/api/wrangler.jsonc`. Worker `/health` returns `database=connected`. Resend is **not** required for preview OTP. |
| **G2** | Drizzle `users` / `email_otp_challenges` / `sessions`. Worker `/health`, `POST /auth/request`, `POST /auth/verify` | Implemented. Neon migrations applied. Preview OTP is returned as `devCode` when Resend is unset. CORS allowlists `*.cyvra-mobile.pages.dev`. |
| **G3** | `apps/web` registration on a Pages preview, then `mobile.cyvra.co.in` | UI exists. Pages project `cyvra-mobile` is Direct Upload today (Git provider = No). Connect Git in the dashboard, or Direct-Upload a preview. Custom domain is out of this slice. |

## Bound live IDs (no secrets)

| Resource | Value |
|---|---|
| Cloudflare account | `5a3eeb2b3d42726a8ba08732464a0eda` |
| Pages | `cyvra-mobile` → `https://cyvra-mobile.pages.dev` |
| Worker | `cyvra-mobile-api` → `https://cyvra-mobile-api.mukpswar.workers.dev` |
| Hyperdrive | `cyvra-mobile-neon` → `db31fc8dafca49b29172da7046b97175` |
| Neon | project `floral-art-02749206`, database `neondb` |

Do not reuse `cyvra-erase-neon-production`, `cyvra-www`, `cyvoriq-erase-api`, `cyvra-admin`, or `cyvra-approvals`.

## Pages Git (dashboard, once)

`cyvra-mobile` was created as a **Direct Uploads** project. The Cloudflare API
rejects attaching a Git `source` (`8000069`). Convert it in the dashboard only:

1. Open **`cyvra-mobile`** (not `cyvra-www`) → Settings → Builds & deployments.
2. Connect Git: repo **`mukpswar-prog/CYVRA-Mobile`**.
3. Production branch: **`main`**. Root directory **empty**.
4. Build command and output are already set on the project (see
   [codespaces-pages.md](./codespaces-pages.md)).
5. Env `VITE_API_URL=https://cyvra-mobile-api.mukpswar.workers.dev` is already
   set on production and preview. No `DATABASE_URL` on Pages.

Until Git is connected, a Direct Upload to **`cyvra-mobile` only** is the
preview path. Never `wrangler pages deploy` to `cyvra-www`.

## Preview OTP

If `cyvra.co.in` is not a verified Resend domain, leave `RESEND_API_KEY` empty
and keep Worker `API_ENV=preview`. The Worker returns `devCode` in JSON; the
web UI shows it. Production (`API_ENV=production`) never returns the code.

Do not attach `mobile.cyvra.co.in` until that DNS step is explicitly in scope.
Do not touch Erase / `cyvra-www` / `cyvoriq-erase-api` / `cyvra-admin` /
`cyvra-approvals`.

Credentials that were pasted into chat should be **rotated** after G1 (Neon
role password + Cloudflare API token), then Hyperdrive origin updated.
