CYVRA — TOKEN INVENTORY (FIND ONLY, DO NOT ROLL ALL)
Date: 10 September 2026
Never paste token values into chat or Git.

================================================================================
0. THE TWO LINES YOU ASKED ABOUT
================================================================================

  read -r -s CLOUDFLARE_API_TOKEN && export CLOUDFLARE_API_TOKEN
  pnpm --filter @cyvra/api exec wrangler secret put ADMIN_API_TOKEN

  These are NOT the same kind of secret.

  1) CLOUDFLARE_API_TOKEN
     What: a Cloudflare **user API token** from
           https://dash.cloudflare.com/profile/api-tokens
     Name to CREATE: cyvoriq-mobile (Edit Cloudflare Workers template).
     Old name cyvra-mobile is now Observability+Hyperdrive only — cannot deploy.
     Job: lets wrangler in Codespaces talk to Cloudflare
          (whoami, deploy, secret put).
     Used by: your terminal / GitHub Codespaces. NOT read by phones or browsers.
     If missing: deploy fails with 6111 / invalid Authorization header.
     Live sites keep running if this token is wrong — you just cannot deploy.

  2) ADMIN_API_TOKEN
     What: a **Worker application secret** we invented for G7.
           It is NOT created on the API Tokens page.
           It is NOT a Cloudflare Pages token.
     Job: Bearer token for POST /admin/serials (ops only).
     Created: you make a long random string (password manager).
     Stored: wrangler secret put ADMIN_API_TOKEN
             on Worker cyvra-mobile-api only.
     If missing after G7 deploy: live /admin/serials returns 503.
     If you never put it: live /admin/serials stays 404 (old Worker) or 503.

  Command order meaning:
    First line  = unlock wrangler (use existing cyvra-mobile token).
    Second line = wrangler, now unlocked, uploads a NEW random ops password
                  into the Worker. You type that NEW string at the secret prompt,
                  not the Cloudflare token again.

================================================================================
1. FIND EVERY CLOUDFLARE *USER* API TOKEN (the list you already opened)
================================================================================

  Open: https://dash.cloudflare.com/profile/api-tokens

  You do not need to Roll. Click the name. Write down only:
    name, permissions, which account, last used.
  Never copy the secret into chat (you cannot see it again anyway unless you Roll).

  From screenshot 10 Sep 2026 19:29 (API Tokens page):

  A. cyvra-mobile
     Permissions shown: Workers Observability, Hyperdrive +1.
     That is NOT Workers Scripts → Edit. It cannot wrangler deploy / secret put.
     9109 on paste = the stored secret is also invalid/old.
     ACTION: do not Roll this. Create a NEW token named cyvoriq-mobile (section 5).
     After whoami works, you may Delete (not Roll) this old cyvra-mobile user token.
     Deleting a USER token does not take mobile.cyvra.co.in or the Worker offline.

  B. cyvra-erase-workers-build
     Account.Workers Scripts. Erase CI.
     DO NOT roll, rename, or delete.

  C. cyvra-erase-production-deploy
     Account.Workers Scripts. Erase production deploy.
     DO NOT roll, rename, or delete.

  D. Cloudflare Agent (conversation) 2026-09-10 and 2026-09-09,
     Cloudflare Agent Token 2026-08-27
     Cursor/agent. DO NOT roll or delete for Mobile.

  E. Workers Builds – 2026-08-27 23:13
     AI Search / directory. Leave it.

  Create (new, cleaner name):

  F. cyvoriq-mobile   ← CREATE THIS NOW
     Template: Edit Cloudflare Workers
     Account: include 5a3eeb2b3d42726a8ba08732464a0eda only
     Zone: include cyvoriq.co.in only (not cyvra.co.in)
     Must show: Account.Workers Scripts (Edit)
     This is CLOUDFLARE_API_TOKEN for Codespaces. Not ADMIN_API_TOKEN.

  If you "Roll all":
    - Running Erase (www, admin, accounts, api.cyvra.co.in) does NOT go down
      at the moment you click Roll.
    - The next Erase deploy / GitHub Action / wrangler that still has the OLD
      erase token WILL fail until you paste the new values into THAT pipeline.
    - That is a real challenge. Do not roll B or C unless you are at the
      Erase repo and ready to update its CI secrets the same hour.
    - Rolling A only is safe for Erase.

================================================================================
2. FIND WORKER SECRETS (not the API Tokens page)
================================================================================

  These live ON a Worker. Pages is a different screen.

  2.1 Mobile Worker  cyvra-mobile-api

    Dashboard:
      https://dash.cloudflare.com/5a3eeb2b3d42726a8ba08732464a0eda/workers/services/view/cyvra-mobile-api/production/settings
      Settings → Variables and Secrets

    Or Codespaces (prints NAMES only if you do not echo values):
      unset CLOUDFLARE_API_TOKEN
      read -r -s CLOUDFLARE_API_TOKEN && export CLOUDFLARE_API_TOKEN
      pnpm --filter @cyvra/api exec wrangler secret list

    Expected names (values stay hidden):

      RESEND_API_KEY      already there (OTP email)
      RESEND_FROM         already there
      SESSION_SECRET      already there (do not rotate unless you want all
                          mobile users signed out)
      ADMIN_API_TOKEN     NOT there yet — this is the new G7 secret
      HYPERDRIVE          binding, not a typed secret
      API_ENV / APP_ORIGIN  vars (preview / pages.dev), not API tokens

    Also confirm you are on Worker **cyvra-mobile-api**.
    Do not open cyvoriq-erase-api.

  2.2 Erase Worker  cyvoriq-erase-api   (LOOK, DO NOT CHANGE)

    Same Cloudflare account → Workers → cyvoriq-erase-api → Settings → Secrets.
    Those secrets belong to mukpswar-prog/Erase. Do not edit for G7.
    Rolling Cloudflare user tokens B/C does not delete these Worker secrets.

================================================================================
3. FIND PAGES ENVIRONMENT VARIABLES (also not API Tokens)
================================================================================

  Cloudflare Pages does not use the API Tokens page at runtime.

  3.1 Mobile Pages  cyvra-mobile

    Workers & Pages → Pages → cyvra-mobile → Settings → Environment variables

    Expected:
      VITE_API_URL = https://cyvra-mobile-api.mukpswar.workers.dev
    Must NOT exist:
      DATABASE_URL, RESEND_API_KEY, ADMIN_API_TOKEN, CLOUDFLARE_API_TOKEN

    ADMIN_API_TOKEN must never go on Pages. The browser would leak it.

  3.2 Erase Pages (LOOK, DO NOT CHANGE)

    cyvra-www          → www.cyvra.co.in
    (admin / accounts Pages projects — whatever names they already have)

    Their VITE_/NEXT_ vars point at api.cyvra.co.in. Leave them.
    Rolling Mobile tokens does not change Pages.

================================================================================
4. SHOULD YOU ROLL EVERYTHING?
================================================================================

  No.

  Roll / create ONLY if you no longer have the cyvra-mobile user token value.
  You already rolled that once. Use the value you copied. Do not roll again
  unless it is lost.

  Never roll cyvra-erase-* for this Mobile task.

  Live Erase challenge if you roll erase tokens anyway:
    1. www.cyvra.co.in and admin / accounts stay online.
    2. Next Erase wrangler deploy or CI job fails auth until you update
       the secret in Erase's GitHub Actions / Codespaces / laptop wrangler.
    3. You must be ready in the Erase repo the same day.
    4. OTP / licences / Windows download are not rewritten by a token roll,
       but you can strand yourself unable to hotfix Erase.

================================================================================
5. WHAT TO DO NOW (ONE BY ONE)
================================================================================

  Do NOT delete live Pages cyvra-mobile, Worker cyvra-mobile-api, Hyperdrive,
  Neon, or mobile.cyvra.co.in. Those are the running product, not "old APIs".
  Cleaning user-token names ≠ deleting the website.

  Step 1  Create Token (user token, dashboard)

          Easiest: Create Token → template **Edit Cloudflare Workers**,
          then name it cyvoriq-mobile and set Zone resources to
          cyvoriq.co.in only.

          If you stay on Create Custom Token (current screen):

          Token name: cyvoriq-mobile   (already filled — keep it)

          Permissions — click + Add more until you have ALL of these
          (the unfinished Zone | Select | Select… row is not enough):

            Account → Workers Scripts     → Edit
            Account → Account Settings    → Read
            Account → Hyperdrive          → Edit
            Zone    → Zone                → Read
            Zone    → DNS                 → Edit
            Zone    → Workers Routes      → Edit

          Account Resources (appears after the Account rows):
            Include → Specific account → Mukpswar@gmail.com's Account
            Do not choose All accounts.

          Zone Resources (already correct on your screenshot):
            Include → Specific zone → cyvoriq.co.in
            Do not add cyvra.co.in.

          Client IP Address Filtering: leave empty (all addresses).

          Continue to summary. You must see Workers Scripts Edit.
          Create Token → copy once into the password manager.

  Step 2  ADMIN_API_TOKEN is NOT on that page.
          Invent a second long random string. Password manager name:
          "mobile ADMIN_API_TOKEN". You will paste it at script prompt 2.

  Step 3  Codespaces (after git pull):

            unset CLOUDFLARE_API_TOKEN
            bash scripts/live-g7-deploy.sh

          Prompt 1 = cyvoriq-mobile user token
          Prompt 2 = invented ADMIN_API_TOKEN
          Expect whoami + deploy + live /admin/serials HTTP 401

  Step 4  After 401: you may Delete the old USER token named cyvra-mobile
          (the Observability one). Live sites stay up.

  Never delete / roll:
    cyvra-erase-workers-build
    cyvra-erase-production-deploy
    Cloudflare Agent *
    Worker cyvra-mobile-api
    Pages cyvra-mobile
    Hyperdrive cyvra-mobile-neon

  Rename of Worker/Pages to cyvoriq-* is cosmetic and comes AFTER
  www.cyvoriq.co.in is proven. Not today.

  Do not put ADMIN_API_TOKEN on Pages.
  Do not put it on cyvoriq-erase-api.
