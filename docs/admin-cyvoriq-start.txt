ADMIN.CYVORIQ.CO.IN — CURRENT DEPLOYMENT RUNBOOK

Product: CYVRA Mobile
Repository: mukpswar-prog/CYVRA-Mobile
Production branch: main
Pages project: cyvoriq-admin
Custom domain: admin.cyvoriq.co.in
API: https://api.cyvoriq.co.in

Governing references:
  docs/CYVRA_MOBILE_PROJECT_INDEX.md
  docs/admin-scope-freeze.md
  docs/neon-cloudflare.md
  docs/resend-mobile-otp.md

Deployment helper:
  scripts/deploy-admin-cyvoriq.sh

SECURITY BOUNDARY

- Never place ADMIN_API_TOKEN on Pages or in browser code.
- Never place DATABASE_URL, RESEND_API_KEY, SESSION_SECRET, or database
  credentials on Pages.
- Normal staff browser access uses the approved staff authentication/session
  flow.
- Break-glass/service credentials are not normal browser authentication.
- CYVRA Mobile admin infrastructure remains separate from CYVRA Erase.

PAGES CONFIGURATION

Project:
  cyvoriq-admin

Production branch:
  main

Framework:
  Vite

Repository root:
  monorepo root

Build command:
  pnpm install && pnpm --filter @cyvra/web build

Build output:
  apps/web/dist

Public build variables:
  VITE_API_URL=https://api.cyvoriq.co.in

Do not place secrets in public build variables.

CUSTOM DOMAIN

After the Pages deployment is healthy:

  Pages -> cyvoriq-admin -> Custom domains
  admin.cyvoriq.co.in

The DNS zone must be cyvoriq.co.in.

VERIFY

  curl.exe -sS -I https://admin.cyvoriq.co.in/

Then verify the current staff authentication flow in the browser.

Do not use an old resume/G0-G8 document as the deployment authority.
