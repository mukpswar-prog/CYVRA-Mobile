# Public web config only — never put secrets or DATABASE_URL here.
# Local:
VITE_API_URL=http://localhost:8787
# Pages cyvoriq-www (set at Vite build time, never committed):
# VITE_API_URL=https://api.cyvoriq.co.in
# Pages preview cyvra-mobile (keep until www is proven):
# VITE_API_URL=https://cyvra-mobile-api.mukpswar.workers.dev
