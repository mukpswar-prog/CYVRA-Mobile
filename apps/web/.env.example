# Public web config only — never put secrets or DATABASE_URL here.
# Local:
VITE_API_URL=http://localhost:8787
# Pages preview (set at Vite build time, never committed):
# VITE_API_URL=https://cyvra-mobile-api.<subdomain>.workers.dev
