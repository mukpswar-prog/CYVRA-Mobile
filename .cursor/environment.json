{
  "name": "CYVRA Mobile Evidence",
  "install": "bash scripts/install.sh",
  "start": "bash scripts/start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; pnpm --filter @cyvra/api dev",
      "description": "Cloudflare Worker API (cyvra-mobile-api) via wrangler dev on http://localhost:8787. Local Postgres stands in for Neon through the Hyperdrive localConnectionString."
    },
    {
      "name": "web",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; pnpm --filter @cyvra/web dev",
      "description": "Customer web app (apps/web) via Vite on http://localhost:5173. Registration / sign-in for mobile.cyvra.co.in."
    }
  ],
  "ports": [
    { "name": "API (Worker)", "port": 8787 },
    { "name": "Web (Vite)", "port": 5173 }
  ]
}
